<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidproject_team">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject_team">
        <activity android:name=".DiaryActivity"></activity>
        <activity android:name=".ListActivity" />
        <activity android:name=".JunjuActivity" />
        <activity android:name=".GyeonggiActivity" />
        <activity android:name=".GangownActivity" />
        <activity android:name=".BusanActivity" />
        <activity android:name=".SeoulActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>